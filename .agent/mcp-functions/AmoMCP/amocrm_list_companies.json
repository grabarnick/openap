{
  "description" : "List companies in amoCRM with optional search and filtering. Supports pagination (max 250), search by keyword. Use \"with\" to include related contacts, leads, customers.",
  "input" : {
    "type" : "object",
    "properties" : {
      "query" : {
        "type" : "string",
        "description" : "Search query string"
      },
      "page" : {
        "type" : "number",
        "description" : "Page number (default: 1)"
      },
      "limit" : {
        "type" : "number",
        "description" : "Results per page, max 250 (default: 50)"
      },
      "with" : {
        "type" : "string",
        "description" : "Comma-separated related entities: catalog_elements, leads, customers, contacts"
      },
      "order" : {
        "type" : "string",
        "description" : "Sort order by updated_at"
      }
    },
    "required" : [ ]
  }
}